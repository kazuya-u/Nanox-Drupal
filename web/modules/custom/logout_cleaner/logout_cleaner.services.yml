services:
  logout_cleaner.logout_event_subscriber:
    class: Drupal\logout_cleaner\EventSubscriber\LogoutEventSubscriber
    arguments: ["@current_user", "@messenger"]
    tags:
      - { name: "event_subscriber" }
